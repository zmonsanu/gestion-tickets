CREATE TABLE IF NOT EXISTS CLIENTS (
    id UUID PRIMARY KEY,
    first VARCHAR(255),
    last VARCHAR(255),
    email VARCHAR(255),
    address VARCHAR(255),
    created VARCHAR(255),
    balance VARCHAR(255)
);
